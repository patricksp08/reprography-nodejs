const mailer = require("./mailer");
const inserirRegistros = require("./inserirRegistros")
const swaggerAuto = require("./swaggerAuto")

module.exports = {
    mailer,
    inserirRegistros,
    swaggerAuto
};